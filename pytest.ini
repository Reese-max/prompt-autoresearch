# 測試指令:
#   pytest                     # 跑全部測試(預設即含 coverage,設定見 .coveragerc)
#   pytest tests/test_xxx.py   # 跑單一測試檔
#   pytest --no-cov            # 關閉 coverage 快速跑
[pytest]
testpaths = tests
addopts = --cov=lib --cov=api --cov=scripts --cov-report=term-missing
