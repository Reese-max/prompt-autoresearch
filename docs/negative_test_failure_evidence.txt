================================================================================
 Red/Green 證據：新增測試揭露的實作缺陷修正
================================================================================

■ 缺陷概述
  1. run_app.py: build_experiment_report_snapshot() 缺少 run_count 驗證——
     空 runs 目錄產生 run_count=0 的無效 snapshot，靜默送出 API。
     修正：加入 if run_count < 1: raise ValueError(...)（L327-331）。
  2. scripts/compare_runs.py: pragmatic 模式 c3 風險條件未要求
     risk_rate >= baseline——risk 從 100% 降至 90% 仍被接受。
     修正：c3 加入 risk_rate >= base_risk_rate（L178）。

================================================================================
 RED（修正前失敗輸出）
================================================================================

tests/test_architecture_surface.py::ArchitectureSurfaceTests::test_experiment_report_snapshot_rejects_semantically_wrong_output FAILED [100%]

================================== FAILURES ===================================
_ ArchitectureSurfaceTests.test_experiment_report_snapshot_rejects_semantically_wrong_output _

self = <test_architecture_surface.ArchitectureSurfaceTests testMethod=test_experiment_report_snapshot_rejects_semantically_wrong_output>

    def test_experiment_report_snapshot_rejects_semantically_wrong_output(self):
        """負向測試：呼叫真正的 build_experiment_report_snapshot，驗證無效輸出被拒絕。
        未修正前，函式無驗證機制——空 runs 產生 run_count=0 的無效 snapshot 會被
        靜默送出 API，此測試因此穩定失敗（raise 被觸發但無 ValueError）。
        修正後，函式應在 output 不符合 contract 時 raise ValueError，此測試通過。"""
        from run_app import build_experiment_report_snapshot
        import scripts.experiment_report as er

        import tempfile

        # 空 runs 目錄 → run_count=0 違反 contract（run_count ≥ 1）
        orig_runs = er.RUNS_DIR
        with tempfile.TemporaryDirectory() as td:
            empty_runs = os.path.join(td, "runs")
            os.makedirs(empty_runs)
            er.RUNS_DIR = empty_runs
            try:
>               with self.assertRaises(ValueError):
E               AssertionError: ValueError not raised

tests\test_architecture_surface.py:184: AssertionError
=============================== 1 failed in 0.32s ===============================

================================================================================
 GREEN（修正後通過輸出）
================================================================================

tests/test_architecture_surface.py::ArchitectureSurfaceTests::test_experiment_report_snapshot_rejects_semantically_wrong_output PASSED [ 33%]
tests/test_compare_runs.py::test_compare_pragmatic_rejects_risk_regression_below_baseline PASSED [ 66%]
tests/test_compare_runs.py::test_mutation_char_count_survives_existence_assertions PASSED [100%]

=============================== 3 passed in 0.60s ===============================

================================================================================
 GREEN（全量測試套件 398 passed, 100% coverage）
================================================================================

============================= test session starts =============================
platform win32 -- Python 3.11.9, pytest-9.1.1
collected 398 items

.................................................. [ 18%]
.................................................. [ 36%]
.................................................. [ 54%]
.................................................. [ 72%]
.................................................. [ 90%]
......................................         [100%]

=============================== tests coverage ================================
TOTAL  1693 stmts  0 miss  100% coverage
398 passed in 14.44s
